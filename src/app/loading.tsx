import { LinearProgress, Stack } from "@mui/material";

const LoadingPage = () => {
  return (
    <Stack mt={"50%"} px={"30%"}>
      <LinearProgress />
    </Stack>
  );
};

export default LoadingPage;
